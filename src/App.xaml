<Application x:Class="Masterstrap.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Masterstrap">
    <Application.Resources>
        <Style x:Key="ChromeButtonBaseStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#404040"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChromeMinimizeButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ChromeButtonBaseStyle}">
            <Setter Property="Content" Value="−"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>

        <Style x:Key="ChromeCloseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ChromeButtonBaseStyle}">
            <Setter Property="Content" Value="×"/>
            <Setter Property="FontSize" Value="22"/>
        </Style>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="{x:Type ToggleButton}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Color x:Key="ColorBgMain">#1C1C1C</Color>
        <Color x:Key="ColorBgPanel">#2D2D2D</Color>
        <Color x:Key="ColorBgSidebar">#1C1C1C</Color>
        <Color x:Key="ColorPrimary">#1084D7</Color>
        <Color x:Key="ColorAccent">#00BCFF</Color>
        <Color x:Key="ColorInfo">#1084D7</Color>
        <Color x:Key="ColorText">#FFFFFF</Color>
        <Color x:Key="ColorTextMuted">#A0A0A0</Color>
        <Color x:Key="ColorBorder">#3A3A3A</Color>
        <Color x:Key="ColorDanger">#FF6B6B</Color>
        <Color x:Key="ColorSuccess">#2ECC71</Color>
        <SolidColorBrush x:Key="BgMain" Color="#1C1C1C" Opacity="0.95"/>
        <SolidColorBrush x:Key="BgPanel" Color="#2D2D2D" Opacity="0.90"/>
        <SolidColorBrush x:Key="BgSidebar" Color="#1C1C1C" Opacity="0.95"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1084D7"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#00BCFF"/>
        <SolidColorBrush x:Key="InfoBrush" Color="#00BCFF"/>
        <SolidColorBrush x:Key="TextMain" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextMuted" Color="#A0A0A0"/>
        <SolidColorBrush x:Key="BorderColor" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="Danger" Color="#FF0000"/>
        <SolidColorBrush x:Key="Success" Color="#2ECC71"/>
        <LinearGradientBrush x:Key="NeonGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#1084D7" Offset="0"/>
            <GradientStop Color="#00BCFF" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="ButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1084D7" Offset="0"/>
            <GradientStop Color="#0A5BB8" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="RobloxCyanGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#1084D7" Offset="0"/>
            <GradientStop Color="#00BCFF" Offset="1"/>
        </LinearGradientBrush>
        <DropShadowEffect x:Key="FloatingGlowPrimary" ShadowDepth="0" BlurRadius="24" Color="#1084D7" Opacity="0.50"/>
        <DropShadowEffect x:Key="FloatingGlowAccent" ShadowDepth="0" BlurRadius="18" Color="#00BCFF" Opacity="0.45"/>
        <DropShadowEffect x:Key="FloatingGlowCyan" ShadowDepth="0" BlurRadius="20" Color="#00BCFF" Opacity="0.45"/>
        <DropShadowEffect x:Key="FloatingGlowDanger" ShadowDepth="0" BlurRadius="16" Color="#FF0000" Opacity="0.50"/>
        <DropShadowEffect x:Key="GlassPanelShadow" ShadowDepth="2" BlurRadius="12" Color="#000000" Opacity="0.5"/>
        <SolidColorBrush x:Key="ComboFaceBrush" Color="#3D3D3D"/>
        <SolidColorBrush x:Key="ComboPopupBrush" Color="#484848"/>
        <SolidColorBrush x:Key="ComboFaceHoverBrush" Color="#4A4A4A"/>
        <SolidColorBrush x:Key="ComboItemHoverBrush" Color="#565656"/>
        <SolidColorBrush x:Key="InteractiveButtonBgBrush" Color="#3A3A3A" Opacity="0.82"/>
        <SolidColorBrush x:Key="GlassPanelBrushBrighter" Color="#2A2A2A" Opacity="0.85"/>
        <SolidColorBrush x:Key="InteractiveButtonBorderBrush" Color="#404040" Opacity="1"/>
        <SolidColorBrush x:Key="InteractiveButtonHoverBrush" Color="#3C3C3C" Opacity="1"/>
        <SolidColorBrush x:Key="InteractiveButtonPressedBrush" Color="#252525" Opacity="1"/>

        <!-- Shared Styles -->
        <Style TargetType="ToggleButton" x:Key="NoFocusToggleButtonStyle">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ApplyPrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#0A0A0A"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="ExtraBold"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Padding" Value="14,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
                            <Border x:Name="FaceBd" CornerRadius="4" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True"
                                    Background="#00D4F0" BorderThickness="1">
                                <Border.BorderBrush>
                                    <SolidColorBrush Color="#0088A8" Opacity="0.45"/>
                                </Border.BorderBrush>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="FaceBd" Property="Background" Value="#33E4FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="FaceBd" Property="Background" Value="#00B8D8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FastFlagEditorButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassmorphicComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ComboFaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMain}"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="35"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="MainBorder"
                                    Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"/>

                            <ToggleButton x:Name="FullAreaToggle"
                                         Grid.ColumnSpan="2"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                         Focusable="False"
                                         Style="{StaticResource NoFocusToggleButtonStyle}"/>

                            <TextBlock x:Name="ContentPresenter"
                                      Grid.Column="0"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      Foreground="{TemplateBinding Foreground}"
                                      IsHitTestVisible="False"
                                      Text="{TemplateBinding SelectionBoxItem}"/>

                            <ToggleButton x:Name="DropdownButton"
                                         Grid.Column="1"
                                         Width="35"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="White"
                                         FontSize="14"
                                         FontWeight="Bold"
                                         IsHitTestVisible="False"
                                         IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                         FocusVisualStyle="{x:Null}"
                                         Style="{StaticResource NoFocusToggleButtonStyle}">▼</ToggleButton>

                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="{DynamicResource ComboPopupBrush}" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="4" Padding="0" Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="200"><ItemsPresenter/></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FooterCloseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextMain}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="FaceBd"
                                Background="{DynamicResource GlassPanelBrushBrighter}"
                                BorderBrush="{DynamicResource InteractiveButtonBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="FaceBd" Property="Background" Value="{DynamicResource InteractiveButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="FaceBd" Property="Background" Value="{DynamicResource InteractiveButtonPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ScrollBar Styles -->
        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Margin="2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>
        <Style x:Key="VerticalScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HorizontalScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Track x:Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                            <ScrollBar Grid.Column="1" Grid.Row="0" Name="PART_VerticalScrollBar" 
                                      Style="{StaticResource VerticalScrollBar}"
                                      Value="{TemplateBinding VerticalOffset}"
                                      Maximum="{TemplateBinding ScrollableHeight}"
                                      ViewportSize="{TemplateBinding ViewportHeight}"
                                      Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar Grid.Column="0" Grid.Row="1" Name="PART_HorizontalScrollBar" 
                                      Style="{StaticResource HorizontalScrollBar}"
                                      Orientation="Horizontal"
                                      Value="{TemplateBinding HorizontalOffset}"
                                      Maximum="{TemplateBinding ScrollableWidth}"
                                      ViewportSize="{TemplateBinding ViewportWidth}"
                                      Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
